# Requirements

- Node > 20 

# Start the project

1. Copy `.env.default` to `.env` and fill missing variables
2. Install dependencies
    ```shell
   npm i
    ```
3. Start project
   ```shell
    npm run dev
    ```